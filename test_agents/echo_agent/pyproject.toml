[project]
name = "echo_agent"
version = "0.1.0"
description = "Minimal echo agent with no tools"
requires-python = ">=3.9"
dependencies = ["openai>=1.0"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["echo_agent"]
